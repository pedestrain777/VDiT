# VBench 视频生成方案详细说明

## 📋 方案概述

### 基本方案
- **Prompt文件数量**: 11个
- **每个文件选择的prompts**: 3个
- **总prompts数**: 11 × 3 = 33个
- **总视频数**: 需要根据维度区分（见下文）

---

## ⚠️ 重要：Temporal Flickering 维度的特殊要求

### 核心差异

| 维度类型 | 每个prompt的视频数 | 命名索引范围 |
|---------|------------------|-------------|
| **Temporal Flickering** | **25个** | 0-24 |
| **其他所有维度** | **5个** | 0-4 |

### 为什么 Temporal Flickering 需要 25 个视频？

**官方说明**（来自 `prompts/README.md`）:
> For the `Temporal Flickering` dimension, sample 25 videos to ensure sufficient coverage after applying the static filter.

**原因**:
1. Temporal Flickering 评估需要先过滤掉静态视频
2. 生成 25 个视频可以确保过滤后仍有足够的样本进行评估
3. 如果只生成 5 个，过滤后可能样本不足，影响评估结果

---

## 📊 修正后的视频数量计算

### 情况 1: 如果选择 temporal_flickering.txt 中的 prompts

假设从 11 个文件中各选 3 个 prompts，其中：
- `temporal_flickering.txt`: 3 个 prompts × 25 个视频 = **75 个视频**
- 其他 10 个文件: 30 个 prompts × 5 个视频 = **150 个视频**
- **总计**: 75 + 150 = **225 个视频**

### 情况 2: 如果不选择 temporal_flickering.txt

假设从 10 个文件中各选 3 个 prompts：
- 30 个 prompts × 5 个视频 = **150 个视频**

### 情况 3: 混合选择（推荐）

**推荐方案**: 如果选择 `temporal_flickering.txt`，只选择 1-2 个 prompts（而不是 3 个）

**示例**:
- `temporal_flickering.txt`: 2 个 prompts × 25 个视频 = **50 个视频**
- 其他 10 个文件: 30 个 prompts × 5 个视频 = **150 个视频**
- **总计**: 50 + 150 = **200 个视频**

---

## 📝 视频命名规范

### 基本格式
```
{prompt文本}-{索引}.mp4
```

### 命名规则

1. **prompt文本**: 必须与 `VBench_full_info.json` 中的 `prompt_en` **完全一致**
   - 包括大小写、标点、空格
   - 不能有任何修改

2. **索引**:
   - **Temporal Flickering**: 0, 1, 2, ..., 24（共 25 个）
   - **其他维度**: 0, 1, 2, 3, 4（共 5 个）

3. **文件扩展名**: `.mp4` 或 `.gif`

### 命名示例

#### 示例 1: subject_consistency.txt（5 个视频）
```
a person swimming in ocean-0.mp4
a person swimming in ocean-1.mp4
a person swimming in ocean-2.mp4
a person swimming in ocean-3.mp4
a person swimming in ocean-4.mp4
```

#### 示例 2: temporal_flickering.txt（25 个视频）
```
In a still frame, a stop sign-0.mp4
In a still frame, a stop sign-1.mp4
In a still frame, a stop sign-2.mp4
...
In a still frame, a stop sign-23.mp4
In a still frame, a stop sign-24.mp4
```

#### 示例 3: scene.txt（5 个视频，注意 prompts 较短）
```
alley-0.mp4
alley-1.mp4
alley-2.mp4
alley-3.mp4
alley-4.mp4
```

#### 示例 4: overall_consistency.txt（5 个视频，注意标点）
```
Close up of grapes on a rotating table.-0.mp4
Close up of grapes on a rotating table.-1.mp4
Close up of grapes on a rotating table.-2.mp4
Close up of grapes on a rotating table.-3.mp4
Close up of grapes on a rotating table.-4.mp4
```

---

## 📁 目录结构方案

### 方案 A: 单文件夹结构（推荐）

所有视频放在同一个文件夹中：

```
your_videos/
├── a person swimming in ocean-0.mp4
├── a person swimming in ocean-1.mp4
├── a person swimming in ocean-2.mp4
├── a person swimming in ocean-3.mp4
├── a person swimming in ocean-4.mp4
├── In a still frame, a stop sign-0.mp4
├── In a still frame, a stop sign-1.mp4
├── In a still frame, a stop sign-2.mp4
├── ...
├── In a still frame, a stop sign-24.mp4
├── alley-0.mp4
├── alley-1.mp4
├── ...
└── (总共 200-225 个视频文件)
```

**优点**:
- ✅ 结构简单
- ✅ 评估时只需指定一个路径
- ✅ 符合 VBench 标准模式的要求

---

## ⚠️ 重要注意事项

### 1. Prompt 文本必须完全匹配

**关键**: 视频文件名中的 prompt 文本必须与 `VBench_full_info.json` 中的 `prompt_en` **完全一致**。

**检查方法**:
```python
import json

with open('VBench-master/vbench/VBench_full_info.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

# 检查你的 prompt 是否存在
your_prompt = "a person swimming in ocean"
exists = any(item['prompt_en'] == your_prompt for item in data)
print(f"Prompt exists: {exists}")

# 如果不存在，查找最接近的
for item in data:
    if your_prompt.lower() in item['prompt_en'].lower():
        print(f"Found similar: {item['prompt_en']}")
```

### 2. 特殊字符处理

如果 prompt 中包含特殊字符，文件名中也要保持一致：

- **逗号**: `,`
- **句号**: `.`
- **空格**: ` ` (空格)
- **其他标点符号**

**示例**:
```
Close up of grapes on a rotating table.-0.mp4  # 注意末尾的句号
```

### 3. 大小写敏感

Prompt 文本是大小写敏感的：

- ✅ `a person swimming in ocean` (正确)
- ❌ `A Person Swimming In Ocean` (错误，大小写不匹配)

### 4. 空格处理

Prompt 中的空格必须保留：

- ✅ `a person swimming in ocean` (正确)
- ❌ `a_person_swimming_in_ocean` (错误，空格被替换)

---

## 📋 完整的视频生成清单示例

### 假设方案：从每个文件选择前 3 个 prompts（temporal_flickering 只选 2 个）

#### 1. subject_consistency.txt (3 个 prompts，每个 5 个视频)
```
a person swimming in ocean → 5 个视频
a person giving a presentation to a room full of colleagues → 5 个视频
a person washing the dishes → 5 个视频
```

#### 2. scene.txt (3 个 prompts，每个 5 个视频)
```
alley → 5 个视频
amusement park → 5 个视频
aquarium → 5 个视频
```

#### 3. overall_consistency.txt (3 个 prompts，每个 5 个视频)
```
Close up of grapes on a rotating table. → 5 个视频
Turtle swimming in ocean. → 5 个视频
A storm trooper vacuuming the beach. → 5 个视频
```

#### 4. temporal_flickering.txt (2 个 prompts，每个 25 个视频) ⚠️
```
In a still frame, a stop sign → 25 个视频
a toilet, frozen in time → 25 个视频
```

#### 5-11. 其他 7 个文件 (每个 3 个 prompts，每个 5 个视频)
```
... (类似结构)
```

**总计**:
- 普通 prompts: 32 个 × 5 = 160 个视频
- Temporal Flickering: 2 个 × 25 = 50 个视频
- **总计**: 210 个视频

---

## 🔄 评估时的维度映射

虽然只有 11 个 prompt 文件，但可以评估 16 个维度：

| Prompt 文件 | 可用于评估的维度 |
|------------|----------------|
| `subject_consistency.txt` | `subject_consistency`, `motion_smoothness`, `dynamic_degree` |
| `scene.txt` | `scene`, `background_consistency` |
| `overall_consistency.txt` | `overall_consistency`, `aesthetic_quality`, `imaging_quality` |
| `temporal_flickering.txt` | `temporal_flickering` ⚠️ (需要 25 个视频) |
| `object_class.txt` | `object_class` |
| `multiple_objects.txt` | `multiple_objects` |
| `color.txt` | `color` |
| `spatial_relationship.txt` | `spatial_relationship` |
| `temporal_style.txt` | `temporal_style` |
| `human_action.txt` | `human_action` |
| `appearance_style.txt` | `appearance_style` |

---

## 💡 推荐方案

### 方案 1: 包含 Temporal Flickering（推荐）

**选择**:
- `temporal_flickering.txt`: **2 个 prompts**（每个 25 个视频）
- 其他 10 个文件: 每个 3 个 prompts（每个 5 个视频）

**视频数量**:
- Temporal Flickering: 2 × 25 = 50 个视频
- 其他: 30 × 5 = 150 个视频
- **总计**: 200 个视频

**优点**:
- ✅ 覆盖所有主要维度
- ✅ 视频数量可控
- ✅ 包含 Temporal Flickering 评估

### 方案 2: 不包含 Temporal Flickering（更简单）

**选择**:
- 从 10 个文件中各选 3 个 prompts（排除 `temporal_flickering.txt`）

**视频数量**:
- 30 × 5 = **150 个视频**

**优点**:
- ✅ 视频数量最少
- ✅ 所有 prompts 统一生成 5 个视频
- ✅ 更简单，不需要区分

**缺点**:
- ❌ 无法评估 Temporal Flickering 维度

---

## 📝 生成脚本示例

```python
import json
from pathlib import Path

# 读取 VBench_full_info.json
vbench_info_path = Path("VBench-master/vbench/VBench_full_info.json")
with open(vbench_info_path, 'r', encoding='utf-8') as f:
    vbench_data = json.load(f)

# 创建 prompt 到维度的映射
prompt_to_dimensions = {}
for item in vbench_data:
    prompt = item['prompt_en']
    dimensions = item.get('dimension', [])
    prompt_to_dimensions[prompt] = dimensions

# 读取选择的 prompts
selected_prompts = [
    "a person swimming in ocean",
    "In a still frame, a stop sign",  # temporal_flickering
    "alley",
    # ... 其他 prompts
]

# 生成视频
for prompt in selected_prompts:
    dimensions = prompt_to_dimensions.get(prompt, [])
    
    # 判断是否需要生成 25 个视频
    if "temporal_flickering" in dimensions:
        num_videos = 25
        print(f"⚠️  {prompt}: 生成 25 个视频 (Temporal Flickering)")
    else:
        num_videos = 5
        print(f"✅ {prompt}: 生成 5 个视频")
    
    for index in range(num_videos):
        video_name = f"{prompt}-{index}.mp4"
        # 生成视频...
        print(f"  生成: {video_name}")
```

---

## ✅ 检查清单

在开始生成视频前，请确认：

- [ ] 已确定是否包含 `temporal_flickering.txt`
- [ ] 如果包含，已确定选择几个 prompts（建议 1-2 个）
- [ ] 已确认每个 prompt 的文本与 `VBench_full_info.json` 完全一致
- [ ] 已设置正确的视频数量（Temporal Flickering: 25，其他: 5）
- [ ] 已确认视频命名格式：`{prompt}-{index}.mp4`
- [ ] 已确认所有视频保存在同一目录下
- [ ] 已准备好评估脚本和输出目录

---

## 📊 最终统计

### 推荐方案（包含 Temporal Flickering）

| 文件类型 | Prompts 数 | 每个视频数 | 总视频数 |
|---------|-----------|-----------|---------|
| temporal_flickering.txt | 2 | 25 | 50 |
| 其他 10 个文件 | 30 | 5 | 150 |
| **总计** | **32** | - | **200** |

### 简化方案（不包含 Temporal Flickering）

| 文件类型 | Prompts 数 | 每个视频数 | 总视频数 |
|---------|-----------|-----------|---------|
| 10 个文件 | 30 | 5 | 150 |
| **总计** | **30** | - | **150** |

---

**最后更新**: 2025-11-15
**基于**: VBench-master 项目代码和文档

